[package]
name = "e2e-tests"
version = "1.0.0"
edition = "2021"

[dependencies]
nikkadb-client = { path = "../nikkadb-client" }
nikkadb-server = { path = "../nikkadb-server" }

[lints]
workspace = true
